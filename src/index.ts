import { processDirectory, watchDirectory } from "./processor";
import { loadConfig, resolveConfig, Config } from "./config";

export { processDirectory, watchDirectory, loadConfig, resolveConfig, Config };
